# P1 阶段：基础框架搭建

**所属项目：** 多智能体辅助在线协作学习平台 v1.2  
**阶段编号：** P1 / 8  
**预计周期：** 1-2 周  
**前置依赖：** 无（起始阶段）

---

## 一、阶段目标

搭建前后端基础项目结构，实现用户认证、房间管理与基础界面，确保核心技术路线完整跑通。

**完成标志：** 演示者可在浏览器中注册账号 → 登录 → 看到房间列表 → 进入房间查看三栏布局界面骨架（聊天区空白但布局比例正确）。

---

## 二、开发方案总览

```
P1 分为 5 个并行或顺序推进的子模块：

  A. 环境与工程初始化
  B. 数据库设计与迁移
  C. 后端 REST API 实现（认证 + 房间）
  D. 前端页面实现（登录 + 大厅 + 骨架）
  E. 联调验证
```

---

## 三、详细开发步骤

### 步骤 1：初始化前端项目

**负责范围：** 前端基础工程

```bash
# 创建 Vue 3 + Vite 项目
npm create vite@latest frontend -- --template vue

cd frontend
npm install

# 安装核心依赖
npm install vue-router@4 pinia axios tailwindcss @tailwindcss/forms
npm install -D @vitejs/plugin-vue autoprefixer postcss

# 初始化 Tailwind
npx tailwindcss init -p
```

**配置要点：**

- `vite.config.js`：配置代理，将 `/api` 和 `/ws` 转发至后端 `http://localhost:8000`
- `tailwind.config.js`：配置 `content` 扫描路径为 `./src/**/*.{vue,js}`
- `src/main.js`：挂载 Pinia、Vue Router

**目录结构初始化：**

```
frontend/src/
├── main.js
├── App.vue
├── router/index.js          ← 定义路由表
├── stores/
│   ├── auth.js              ← 认证 Store
│   └── room.js              ← 房间 Store（占位）
├── views/
│   ├── LoginView.vue
│   ├── LobbyView.vue
│   └── RoomView.vue         ← 三栏骨架
├── components/
│   ├── layout/
│   ├── chat/
│   ├── editor/
│   └── task/
└── services/
    ├── api.js               ← Axios 实例
    └── authApi.js
```

---

### 步骤 2：初始化后端项目

**负责范围：** 后端基础工程

```bash
# 创建目录并初始化虚拟环境
mkdir backend && cd backend
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate

# 安装核心依赖
pip install fastapi uvicorn[standard] sqlalchemy[asyncio] asyncpg
pip install alembic redis pydantic[email] pydantic-settings python-jose[cryptography]
pip install bcrypt pyyaml python-dotenv anthropic apscheduler

pip freeze > requirements.txt
```

**目录结构初始化（P1 阶段仅实现核心部分）：**

```
backend/
├── app/
│   ├── main.py              ← FastAPI 应用入口
│   ├── config.py            ← 环境变量加载
│   ├── dependencies.py      ← 通用依赖（get_db, get_current_user）
│   ├── routers/
│   │   ├── auth.py          ← /api/auth/*
│   │   └── rooms.py         ← /api/rooms/*
│   ├── models/
│   │   ├── user.py
│   │   ├── room.py
│   │   ├── room_member.py
│   │   └── task.py
│   ├── schemas/
│   │   ├── auth.py
│   │   └── room.py
│   ├── services/
│   │   ├── auth_service.py
│   │   └── room_service.py
│   └── db/
│       ├── session.py       ← SQLAlchemy 异步会话
│       └── redis_client.py  ← Redis 客户端（占位）
├── alembic/
├── .env
├── .env.example
└── requirements.txt
```

---

### 步骤 3：搭建 Docker Compose（纯数据库模式）

**文件：** `docker-compose.yml`（项目根目录）

```yaml
version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: collab
      POSTGRES_PASSWORD: collab_pass
      POSTGRES_DB: collab_db
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  pg_data:
```

> **说明：** P1 阶段 Docker Compose 仅启动 PostgreSQL 和 Redis。前端与后端均在本地手动启动以支持热重载。`y-websocket` 服务在 P6 阶段协同编辑功能开发时加入。

**启动命令：**

```bash
docker compose up -d
# 验证
docker compose ps
```

---

### 步骤 4：数据库 ORM 模型设计

**文件：** `backend/app/models/`

#### `user.py`

```python
from sqlalchemy import Column, String, Enum, DateTime
from sqlalchemy.dialects.postgresql import UUID
import uuid, enum

class UserRole(str, enum.Enum):
    student = "student"
    teacher = "teacher"

class User(Base):
    __tablename__ = "users"
    
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    username = Column(String(50), unique=True, nullable=False, index=True)
    password_hash = Column(Text, nullable=False)
    role = Column(Enum(UserRole), nullable=False)
    display_name = Column(String(100), nullable=False)
    created_at = Column(DateTime(timezone=True), server_default=func.now())
```

#### `room.py`

```python
class RoomStatus(str, enum.Enum):
    waiting = "waiting"
    active = "active"
    ended = "ended"

class Room(Base):
    __tablename__ = "rooms"
    
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    name = Column(String(200), nullable=False)
    task_id = Column(UUID(as_uuid=True), ForeignKey("tasks.id"), nullable=True)
    created_by = Column(UUID(as_uuid=True), ForeignKey("users.id"), nullable=False)
    status = Column(Enum(RoomStatus), default=RoomStatus.waiting)
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    ended_at = Column(DateTime(timezone=True), nullable=True)
```

#### `room_member.py`

```python
class RoomMember(Base):
    __tablename__ = "room_members"
    
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    room_id = Column(UUID(as_uuid=True), ForeignKey("rooms.id"), nullable=False)
    user_id = Column(UUID(as_uuid=True), ForeignKey("users.id"), nullable=False)
    joined_at = Column(DateTime(timezone=True), server_default=func.now())
    
    __table_args__ = (UniqueConstraint("room_id", "user_id"),)
```

#### `task.py`

```python
class Task(Base):
    __tablename__ = "tasks"
    
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    title = Column(Text, nullable=False)
    requirements = Column(Text)
    scripts = Column(JSONB)
    discussion_template = Column(Text)
    created_by = Column(UUID(as_uuid=True), ForeignKey("users.id"), nullable=False)
```

#### `__init__.py`（必须创建）

`Room` 的外键依赖 `tasks` 表，Alembic autogenerate 时若 import 顺序不当会生成错误的迁移文件。需在 `models/__init__.py` 中按依赖顺序显式导入所有模型：

```python
# models/__init__.py
from .user import User
from .task import Task
from .room import Room
from .room_member import RoomMember
```

`alembic/env.py` 中统一使用 `from app.models import *` 触发导入，确保 Alembic 能感知到所有表的依赖顺序。

---

### 步骤 5：Alembic 数据库迁移

```bash
cd backend
alembic init alembic

# 编辑 alembic/env.py：
# - 导入所有 Model 类
# - 设置 target_metadata = Base.metadata
# - 配置异步连接：使用 asyncpg DSN

# 生成首次迁移
alembic revision --autogenerate -m "init_tables"

# 执行迁移
alembic upgrade head
```

**`alembic/env.py` 异步配置要点：**

```python
from app.models import Base  # 确保所有 model 被 import

target_metadata = Base.metadata

def run_migrations_online():
    connectable = create_async_engine(settings.DATABASE_URL)
    
    async def do_run_migrations(connection):
        await connection.run_sync(do_run_migrations_sync)
    
    async def main():
        async with connectable.connect() as connection:
            await do_run_migrations(connection)
    
    asyncio.run(main())
```

---

### 步骤 6：实现认证模块（后端）

**文件：** `backend/app/routers/auth.py` + `backend/app/services/auth_service.py`

#### API 端点

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/register` | 注册（无需认证） |
| POST | `/api/auth/login` | 登录，返回 JWT Token |
| GET | `/api/auth/me` | 获取当前用户信息（需 JWT） |

#### 实现细节

**密码加密：** 使用 `bcrypt` 对密码进行哈希

```python
import bcrypt

def hash_password(plain: str) -> str:
    return bcrypt.hashpw(plain.encode(), bcrypt.gensalt()).decode()

def verify_password(plain: str, hashed: str) -> bool:
    return bcrypt.checkpw(plain.encode(), hashed.encode())
```

**JWT 生成：** 使用 `python-jose` 生成带过期时间的 JWT

```python
from jose import jwt
from datetime import datetime, timedelta

SECRET_KEY = settings.JWT_SECRET_KEY
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 60 * 24 * 7  # 7天

def create_access_token(user_id: str, role: str) -> str:
    expire = datetime.utcnow() + timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES)
    return jwt.encode(
        {"sub": user_id, "role": role, "exp": expire},
        SECRET_KEY, algorithm=ALGORITHM
    )
```

**依赖函数：** `dependencies.py` 中实现 `get_current_user`

```python
async def get_current_user(
    token: str = Depends(oauth2_scheme),
    db: AsyncSession = Depends(get_db)
) -> User:
    # 解码 JWT → 查询用户 → 返回 User 对象
    # 验证失败时抛出 401 HTTPException
```

**角色权限依赖：**

```python
def require_teacher(current_user: User = Depends(get_current_user)) -> User:
    if current_user.role != UserRole.teacher:
        raise HTTPException(status_code=403, detail="需要教师权限")
    return current_user
```

---

### 步骤 7：实现房间模块（后端）

**文件：** `backend/app/routers/rooms.py` + `backend/app/services/room_service.py`

#### API 端点

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/rooms` | 获取房间列表（支持 `?status=active`） | JWT |
| POST | `/api/rooms` | 创建房间 | JWT + 教师 |
| GET | `/api/rooms/{room_id}` | 获取房间详情 | JWT |
| POST | `/api/rooms/{room_id}/join` | 加入房间 | JWT |
| PATCH | `/api/rooms/{room_id}` | 更新状态 | JWT + 教师 |

#### P1 阶段实现范围

- 创建房间时 `task_id` 可为空（P1 阶段任务模块未实现）
- 加入房间写入 `room_members`，遇到重复键忽略（UNIQUE 约束处理）
- 房间列表支持 `status` 过滤，默认返回所有非 `ended` 房间

---

### 步骤 8：任务模块基础实现（后端）

**文件：** `backend/app/routers/tasks.py`

P1 阶段仅实现以下端点，供创建房间时关联使用：

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/tasks` | 获取任务列表 |
| POST | `/api/tasks` | 创建任务（教师） |
| GET | `/api/tasks/{task_id}` | 获取任务详情 |

---

### 步骤 9：前端认证页面实现

**文件：** `frontend/src/views/LoginView.vue`

**功能点：**
- 登录表单（用户名 + 密码）和注册表单（Tab 切换）
- 表单验证：字段非空、密码最小长度 6 位
- 调用 `authApi.login()` / `authApi.register()`
- 登录成功后将 JWT Token 和 user 对象同时存入 `localStorage`（`localStorage.setItem('token', token)` + `localStorage.setItem('user', JSON.stringify(user))`）
- Pinia `auth store` 更新 `user` 和 `token` 状态
- 根据 `role` 字段跳转：`student → /lobby`，`teacher → /dashboard`

**`stores/auth.js` 核心状态：**

```javascript
export const useAuthStore = defineStore('auth', {
  state: () => ({
    // 应用启动时从 localStorage 恢复，确保刷新页面不掉登录态
    user: JSON.parse(localStorage.getItem('user')) || null,
    token: localStorage.getItem('token') || null,
  }),
  getters: {
    isAuthenticated: (state) => !!state.token,
    isTeacher: (state) => state.user?.role === 'teacher',
  },
  actions: {
    async login(username, password) { /* ... */ },
    async register(data) { /* ... */ },
    logout() {
      this.user = null;
      this.token = null;
      localStorage.removeItem('token');
      localStorage.removeItem('user');
    }
  }
})
```

---

### 步骤 10：前端大厅页面实现

**文件：** `frontend/src/views/LobbyView.vue`

**功能点：**
- 页面加载时调用 `GET /api/rooms` 获取房间列表
- 展示房间卡片：房间名称、状态徽标（等待中/进行中）、人数
- 点击"加入"按钮：调用 `POST /api/rooms/{room_id}/join` → 跳转至 `/room/{room_id}`
- 教师用户额外显示"创建房间"按钮（打开 Modal）

---

### 步骤 11：前端主协作页面骨架实现

**文件：** `frontend/src/views/RoomView.vue`

**P1 阶段仅实现布局骨架：**

```
┌──────────────────────────────────────────────────────┐
│  左栏 (35%)          │  中栏 (35%)  │  右栏 (30%)   │
│  ┌────────────────┐  │              │  ┌──────────┐  │
│  │  ① 讨论区      │  │  ③ 聊天区   │  │④ 任务要求│  │
│  │  [占位文字]    │  │  [消息列表] │  │ [占位]   │  │
│  │                │  │  (空)       │  │          │  │
│  └────────────────┘  │              │  └──────────┘  │
│  ┌────────────────┐  │  [输入框]   │  ┌──────────┐  │
│  │  ② 写作区      │  │              │  │⑤ 任务脚本│  │
│  │  [占位文字]    │  │              │  │ [占位]   │  │
│  └────────────────┘  │              │  └──────────┘  │
└──────────────────────────────────────────────────────┘
```

**TailwindCSS 布局实现：**

```vue
<template>
  <div class="flex h-screen overflow-hidden bg-gray-50">
    <!-- 左栏 35% -->
    <div class="w-[35%] flex flex-col gap-2 p-2 border-r border-gray-200">
      <DiscussionArea class="flex-1" />
      <WritingArea class="flex-1" />
    </div>
    
    <!-- 中栏 35% -->
    <div class="w-[35%] flex flex-col border-r border-gray-200">
      <ChatPanel class="flex-1" />
    </div>
    
    <!-- 右栏 30% -->
    <div class="w-[30%] flex flex-col gap-2 p-2">
      <TaskRequirements class="flex-1" />
      <TaskScript class="flex-1" />
    </div>
  </div>
</template>
```

各子区域 P1 阶段均为占位组件，内容为静态文字，供后续阶段逐步填充。

---

### 步骤 12：环境变量配置

**文件：** `backend/.env.example`

```env
DATABASE_URL=postgresql+asyncpg://collab:collab_pass@localhost:5432/collab_db
REDIS_URL=redis://localhost:6379/0
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxx
JWT_SECRET_KEY=请替换为64位随机字符串
CORS_ORIGINS=http://localhost:5173
YJS_WEBSOCKET_PORT=1234
```

**后端 `config.py` 加载：**

```python
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    DATABASE_URL: str
    REDIS_URL: str
    ANTHROPIC_API_KEY: str
    JWT_SECRET_KEY: str
    CORS_ORIGINS: str = "http://localhost:5173"
    
    class Config:
        env_file = ".env"

settings = Settings()
```

---

### 步骤 13：FastAPI 主入口配置

**文件：** `backend/app/main.py`

```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from contextlib import asynccontextmanager
from app.routers import auth, rooms, tasks

@asynccontextmanager
async def lifespan(app: FastAPI):
    # P1 阶段：仅初始化数据库连接
    yield
    # 关闭时清理资源

app = FastAPI(title="多智能体协作学习平台", version="1.2.0", lifespan=lifespan)

app.add_middleware(
    CORSMiddleware,
    allow_origins=settings.CORS_ORIGINS.split(","),
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

app.include_router(auth.router, prefix="/api/auth", tags=["认证"])
app.include_router(rooms.router, prefix="/api/rooms", tags=["房间"])
app.include_router(tasks.router, prefix="/api/tasks", tags=["任务"])
```

---

### 步骤 14：前后端联调验证

**验证清单：**

| 验证项 | 方法 | 预期结果 |
|--------|------|----------|
| 后端启动正常 | `uvicorn app.main:app --reload` | 无报错，访问 `/docs` 有 Swagger UI |
| 数据库连接正常 | 访问 `/api/auth/register` | 成功写入 users 表 |
| JWT 认证正常 | 登录后携带 token 访问 `/api/auth/me` | 返回用户信息 |
| 前端跨域正常 | 前端调用 API | 无 CORS 报错 |
| 房间创建正常 | 教师账号创建房间 | 数据库写入，前端列表刷新 |
| 加入房间正常 | 学生账号加入房间 | 写入 room_members，跳转布局页 |
| 三栏布局正常 | 进入房间 | 三栏宽度比例 35%/35%/30% 正确 |

---

## 四、关键技术细节

### 4.1 Pinia 路由守卫

```javascript
// router/index.js
router.beforeEach((to, from) => {
  const authStore = useAuthStore()
  if (to.meta.requiresAuth && !authStore.isAuthenticated) {
    return '/login'
  }
  if (to.meta.requiresTeacher && !authStore.isTeacher) {
    return '/lobby'
  }
})
```

### 4.2 Axios 请求拦截器

```javascript
// services/api.js
const api = axios.create({ baseURL: '/api' })

api.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) config.headers.Authorization = `Bearer ${token}`
  return config
})

api.interceptors.response.use(
  response => response.data,
  error => {
    if (error.response?.status === 401) {
      // 清除登录状态，跳转登录页
    }
    return Promise.reject(error)
  }
)
```

### 4.3 SQLAlchemy 异步会话

```python
# db/session.py
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession, async_sessionmaker

engine = create_async_engine(settings.DATABASE_URL, echo=False)
AsyncSessionLocal = async_sessionmaker(engine, expire_on_commit=False)

async def get_db():
    async with AsyncSessionLocal() as session:
        yield session
```

---

## 五、Pydantic Schema 设计（关键）

```python
# schemas/auth.py
class RegisterRequest(BaseModel):
    username: str = Field(min_length=3, max_length=50)
    password: str = Field(min_length=6)
    display_name: str = Field(max_length=100)
    role: UserRole = UserRole.student

class LoginRequest(BaseModel):
    username: str
    password: str

class TokenResponse(BaseModel):
    access_token: str
    token_type: str = "bearer"
    user: UserResponse

class UserResponse(BaseModel):
    id: UUID
    username: str
    display_name: str
    role: UserRole
    
    model_config = ConfigDict(from_attributes=True)
```

---

## 六、演示交付物

**P1 演示版本 — 基础架构 Demo**

**演示步骤：**
1. 打开浏览器，访问 `http://localhost:5173`
2. 注册一个教师账号（username: `teacher1`，role: `teacher`）
3. 注册 2-3 个学生账号（role: `student`）
4. 教师账号登录 → 创建一个房间（名称："第一组讨论室"）
5. 学生账号登录 → 在大厅看到房间列表 → 点击加入
6. 进入房间后看到三栏布局骨架（所有区域为占位文字）

**验收指标：**
- [ ] 注册/登录无报错
- [ ] JWT 持久化（刷新页面不掉登录态）
- [ ] 房间列表正确显示
- [ ] 三栏宽度比例视觉正确
- [ ] Docker 中 PostgreSQL 数据持久化（重启容器后数据保留）

---

## 七、P1 → P2 交接说明

P1 结束后，以下基础设施已就绪，可供 P2 直接使用：

- ✅ `users` / `rooms` / `room_members` / `tasks` 表已建立
- ✅ JWT 认证体系已就绪（P2 WebSocket 鉴权将复用 JWT 解码逻辑）
- ✅ 前端 `auth store` 和 `router` 守卫已就绪
- ✅ `RoomView.vue` 骨架已就绪（P2 填充 ChatPanel）
- ✅ Redis 容器已启动（P2 用于 Pub/Sub）

P2 阶段需新增：`messages` 表迁移、WebSocket 端点实现、Redis Pub/Sub 配置。
