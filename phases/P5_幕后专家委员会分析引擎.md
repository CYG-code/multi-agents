# P5 闃舵锛氬箷鍚庝笓瀹跺鍛樹細锛堝垎鏋愬紩鎿庯級寮€鍙?
**鎵€灞為」鐩細** 澶氭櫤鑳戒綋杈呭姪鍦ㄧ嚎鍗忎綔瀛︿範骞冲彴 v1.2  
**闃舵缂栧彿锛?* P5 / 8  
**棰勮鍛ㄦ湡锛?* 1-2 鍛? 
**鍓嶇疆渚濊禆锛?* P4锛圓gentWorker銆佷换鍔￠槦鍒椼€?涓鑹睞gent銆佸熀纭€鐗圕ommittee锛?
---

## 涓€銆侀樁娈电洰鏍?
灏?P4 闃舵鐨勭粺璁¤鍒欏鍛樹細鏇挎崲涓虹湡姝ｇ殑 AI 鍒嗘瀽寮曟搸锛氫笁浣?Analyst 骞跺彂璋冪敤 Claude API 杈撳嚭缁撴瀯鍖栬瘖鏂姤鍛婏紝ChiefDispatcher 缁煎悎涓変唤鎶ュ憡杈撳嚭璋冨害鎸囦护銆傚垎鏋愮粨鏋滃畬鏁存寔涔呭寲锛孊 绫?AI 椹卞姩璋冨害涓?C 绫讳富鍔ㄥ彫鍞ゅ畬鏁撮獙璇併€?
**瀹屾垚鏍囧織锛?* 瑙﹀彂涓€娆″畾鏃跺垎鏋愶紝鍚庡彴鏃ュ織鏄剧ず涓変綅 Analyst 骞跺彂瀹屾垚锛屾暀甯堢鍑虹幇鍖呭惈涓変唤涓撳璇婃柇鐨勫垎鏋愬揩鐓э紝瀛︾敓 @Agent 鍚庤祫婧愯€呯珛鍗冲搷搴斻€?
---

## 浜屻€佸紑鍙戞柟妗堟€昏

```
P5 鍒嗕负 5 涓瓙绯荤粺锛?
  A. 鏁版嵁搴撴墿灞曪紙analysis_snapshots + agent_interventions 琛級
  B. 涓変綅鍒嗘瀽涓撳 AI 瀹炵幇锛圕laude API 缁撴瀯鍖栬緭鍑猴級
  C. ChiefDispatcher 瀹炵幇锛堟眹鎬讳笁浠芥姤鍛?鈫?璋冨害鎸囦护锛?  D. committee.py 缂栨帓灞傦紙asyncio.gather + 鏁版嵁搴撳啓鍏ユ祦姘达級
  E. 瀹屾暣 B 绫昏Е鍙?+ 鍒嗘瀽 API + WebSocket 鎺ㄩ€?```

---

## 涓夈€佹暟鎹簱鎵╁睍

```bash
alembic revision --autogenerate -m "add_analysis_tables"
alembic upgrade head
```

**`backend/app/models/analysis.py`锛?*

```python
import enum
from sqlalchemy import Column, Text, Float, Integer, BigInteger, Enum, ForeignKey, DateTime
from sqlalchemy.dialects.postgresql import UUID, JSONB
import uuid

class TriggeredBy(str, enum.Enum):
    timer = "timer"
    event = "event"

class AnalysisSnapshot(Base):
    __tablename__ = "analysis_snapshots"
    
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    room_id = Column(UUID(as_uuid=True), ForeignKey("rooms.id"), nullable=False)
    message_seq_from = Column(BigInteger, nullable=False)
    message_seq_to = Column(BigInteger, nullable=False)
    analyzed_message_count = Column(Integer, nullable=False)
    
    # 鍚?Analyst 杈撳嚭
    participation_scores = Column(JSONB)
    diversity_score = Column(Float)
    balance_score = Column(Float)
    progress_score = Column(Float)
    emotion_flags = Column(JSONB)
    
    # 鍚?Analyst 鍘熷鎶ュ憡
    cognitive_report = Column(JSONB)
    emotional_report = Column(JSONB)
    interaction_report = Column(JSONB)
    
    # ChiefDispatcher 姹囨€?    dispatcher_summary = Column(Text)
    
    triggered_by = Column(Enum(TriggeredBy), nullable=False)
    created_at = Column(DateTime(timezone=True), server_default=func.now())


class AgentIntervention(Base):
    __tablename__ = "agent_interventions"
    
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    room_id = Column(UUID(as_uuid=True), ForeignKey("rooms.id"), nullable=False)
    snapshot_id = Column(UUID(as_uuid=True), ForeignKey("analysis_snapshots.id"), nullable=True)
    agent_role = Column(String(50), nullable=False)
    reason = Column(Text, nullable=False)
    message_id = Column(UUID(as_uuid=True), ForeignKey("messages.id"), nullable=True)
    created_at = Column(DateTime(timezone=True), server_default=func.now())
```

---

## 鍥涖€佽缁嗗紑鍙戞楠?
### 姝ラ 1锛氬疄鐜?metrics.py锛堝彲閲忓寲鎸囨爣璁＄畻锛?
**鏂囦欢锛?* `backend/app/analysis/metrics.py`

```python
from collections import Counter
from app.models.message import Message

def calculate_participation_scores(messages: list[dict]) -> dict:
    """
    璁＄畻鍚勬垚鍛樺弬涓庡害寰楀垎锛堝彂瑷€娆℃暟褰掍竴鍖栵級銆?    杩斿洖 {user_id: score} 鏍煎紡銆?    """
    student_msgs = [m for m in messages if m.get("sender_type") == "student"]
    counts = Counter(m.get("sender_id") for m in student_msgs)
    
    if not counts:
        return {}
    
    total = sum(counts.values())
    return {uid: count / total for uid, count in counts.items()}


def calculate_balance_score(participation_scores: dict) -> float:
    """
    鍗忎綔鍧囪　鎬?= 1 - 鍩哄凹绯绘暟锛堣秺鍧囪　瓒婃帴杩?锛夈€?    """
    if not participation_scores:
        return 0.5
    
    values = sorted(participation_scores.values())
    n = len(values)
    
    # 鍩哄凹绯绘暟璁＄畻
    cumulative = sum((i + 1) * v for i, v in enumerate(values))
    total = sum(values)
    
    if total == 0:
        return 0.5
    
    gini = (2 * cumulative) / (n * total) - (n + 1) / n
    return max(0.0, min(1.0 - abs(gini), 1.0))


def extract_message_stats(messages: list[dict]) -> dict:
    """鎻愬彇娑堟伅缁熻淇℃伅锛屼緵 Analyst 浣跨敤"""
    student_msgs = [m for m in messages if m.get("sender_type") == "student"]
    
    return {
        "total_count": len(messages),
        "student_count": len(student_msgs),
        "agent_count": len(messages) - len(student_msgs),
        "unique_participants": len(set(m.get("sender_id") for m in student_msgs if m.get("sender_id"))),
        "avg_message_length": sum(len(m.get("content", "")) for m in student_msgs) / max(len(student_msgs), 1),
    }
```

---

### 姝ラ 2锛氬疄鐜颁笁浣嶅垎鏋愪笓瀹?Prompt

**鐩綍锛?* `backend/app/agents/prompts/analysts/`

#### `cognitive_analyst_prompt.txt`

```
浣犳槸涓€涓湪绾垮崗浣滃涔犱笓瀹讹紝涓撻棬鍒嗘瀽瀛︾敓鐨勮鐭ュ眰闈㈠崗浣滆川閲忋€?
浣犵殑浠诲姟鏄垎鏋愪互涓嬭亰澶╄褰曪紝杈撳嚭缁撴瀯鍖?JSON 璇婃柇鎶ュ憡銆?
銆愬垎鏋愮淮搴︺€?1. diversity_score锛?-1锛夛細璁虹偣澶氭牱鎬?   - 0.0: 鎵€鏈変汉瑙傜偣楂樺害涓€鑷达紝鏃犱笉鍚屽０闊?   - 0.5: 瀛樺湪 2-3 绉嶄笉鍚岃鐐?   - 1.0: 瑙傜偣涓板瘜澶氬厓锛屾繁搴﹁璁?   
2. progress_score锛?-1锛夛細浠诲姟杩涘睍绋嬪害
   - 鏍规嵁璁ㄨ鍐呭涓庝换鍔¤姹傜殑鍖归厤搴﹁瘎浼?
銆愯緭鍏ユ暟鎹€?浠诲姟瑕佹眰锛歿task_requirements}
褰撳墠闃舵锛歿current_phase}
鍙備笌鑰呯粺璁★細{participant_stats}

鑱婂ぉ璁板綍锛堟渶杩憑message_count}鏉★級锛?{chat_history}

銆愯緭鍑鸿姹傘€?涓ユ牸杈撳嚭 JSON锛屼笉瑕佹坊鍔犱换浣曞墠缂€璇存槑锛?{
  "diversity_score": <0鍒?鐨勬诞鐐规暟>,
  "progress_score": <0鍒?鐨勬诞鐐规暟>,
  "cognitive_report": {
    "dominant_viewpoints": ["瑙傜偣1", "瑙傜偣2"],
    "missing_perspectives": ["缂哄け瑙掑害1"],
    "task_alignment": "<楂?涓?浣?",
    "diagnosis": "<100瀛椾互鍐呯殑璁ょ煡灞傚垎鏋?",
    "recommendation": "<鍏蜂綋寤鸿>"
  }
}
```

#### `emotional_analyst_prompt.txt`

```
浣犳槸涓€涓崗浣滃涔犳儏缁垎鏋愪笓瀹讹紝涓撻棬璇嗗埆瀛︾敓鍗忎綔涓殑鎯呯华鐘舵€佷笌浜洪檯鍏崇郴璐ㄩ噺銆?
銆愬垎鏋愮淮搴︺€?emotion_flags锛堜笁涓竷灏斿€硷級锛?- conflict: 鏄惁瀛樺湪鏄庢樉鐨勮鐐瑰啿绐佹垨瑷€璇懇鎿?- passive: 鏄惁鏈夋垚鍛樿〃鐜板嚭娑堟瀬銆佹棤鍙備笌鎰忔効
- anxious: 鏄惁鏈夋垚鍛樿〃鐜板嚭鐒﹁檻鎴栧浠诲姟鐨勫帇杩劅

銆愯緭鍏ユ暟鎹€?鍙備笌鑰呭垪琛細{members_info}
鑱婂ぉ璁板綍锛堟渶杩憑message_count}鏉★級锛?{chat_history}

銆愯緭鍑鸿姹傘€?涓ユ牸杈撳嚭 JSON锛?{
  "emotion_flags": {
    "conflict": <true/false>,
    "passive": <true/false>,
    "anxious": <true/false>
  },
  "emotional_report": {
    "overall_tone": "<绉瀬/涓€?娑堟瀬>",
    "risk_members": ["鎴愬憳鏄剧ず鍚?],
    "conflict_details": "<鑻ュ瓨鍦ㄥ啿绐侊紝鎻忚堪鍐茬獊鍐呭>",
    "diagnosis": "<100瀛椾互鍐呯殑鎯呯华灞傚垎鏋?",
    "recommendation": "<鍏蜂綋寤鸿>"
  }
}
```

#### `interaction_analyst_prompt.txt`

```
浣犳槸涓€涓崗浣滀簰鍔ㄨ川閲忓垎鏋愪笓瀹讹紝涓撻棬鍒嗘瀽瀛︾敓鍙備笌搴︿笌鍗忎綔鍧囪　鎬с€?
銆愬垎鏋愮淮搴︺€?1. participation_scores锛氬悇鎴愬憳鍙備笌搴︼紙鐩稿姣旈噸锛?2. balance_score锛?-1锛夛細鍗忎綔鍧囪　鎬?   - 鎺ヨ繎0锛氶珮搴︿笉鍧囪　锛堜竴浜哄瀯鏂級
   - 鎺ヨ繎1锛氭墍鏈変汉鍧囪　鍙備笌

銆愯緭鍏ユ暟鎹€?鍙備笌鑰呭垪琛細{members_info}
鍙戣█缁熻锛歿speaker_stats}
鑱婂ぉ璁板綍锛堟渶杩憑message_count}鏉★級锛?{chat_history}

銆愯緭鍑鸿姹傘€?涓ユ牸杈撳嚭 JSON锛?{
  "participation_scores": {"<user_display_name>": <0鍒?鐨勬瘮閲?},
  "balance_score": <0鍒?鐨勬诞鐐规暟>,
  "interaction_report": {
    "silent_members": ["闀挎湡鏈彂瑷€鎴愬憳鍚?],
    "dominant_members": ["鍙戣█杩囧鎴愬憳鍚?],
    "interaction_quality": "<娣卞害/琛ㄥ眰/鍗曞悜>",
    "diagnosis": "<100瀛椾互鍐呯殑浜掑姩灞傚垎鏋?",
    "recommendation": "<鍏蜂綋寤鸿>"
  }
}
```

#### `chief_dispatcher_prompt.txt`

```
浣犳槸鍗忎綔瀛︿範绯荤粺鐨勯甯皟搴﹀畼銆備綘涓嶇洿鎺ユ帴瑙﹀師濮嬭亰澶╄褰曪紝鍙熀浜庝笁浣嶄笓瀹剁殑鍒嗘瀽鎶ュ憡杩涜缁煎悎瑁佸畾銆?
浣犵殑鑱岃矗锛?1. 缁煎悎涓変唤涓撳鎶ュ憡锛岀粰鍑?00瀛椾互鍐呯殑鎬讳綋璇婃柇
2. 鍐冲畾鏄惁闇€瑕佹淳閬?AI 瑙掕壊浠嬪叆锛屼互鍙婃淳閬ｅ摢涓鑹?
鍙淳閬ｈ鑹诧細
- facilitator锛堜富鎸佷汉锛夛細寮曞鏂瑰悜銆佹墦鐮存矇榛?- devil_advocate锛堟壒鍒よ€咃級锛氭縺鍙戝鍏冩€濊€?- summarizer锛堟€荤粨鑰咃級锛氬綊绾冲叡璇嗐€佹帹杩涢樁娈?- resource_finder锛堣祫婧愯€咃級锛氳ˉ鍏呯煡璇嗕俊鎭?- encourager锛堟縺鍔辫€咃級锛氶個璇锋矇榛樻垚鍛樸€佽皟鑺傛儏缁?
銆愬綋鍓嶅崗浣滅姸鎬併€?闃舵锛歿current_phase}
宸茬敤鏃讹細{elapsed_minutes} 鍒嗛挓

銆愪笁浠戒笓瀹舵姤鍛娿€?璁ょ煡鍒嗘瀽甯堟姤鍛婏細{cognitive_report}
鎯呯华鍒嗘瀽甯堟姤鍛婏細{emotional_report}
浜掑姩鍒嗘瀽甯堟姤鍛婏細{interaction_report}

銆愯緭鍑鸿姹傘€?涓ユ牸杈撳嚭 JSON锛堜笉瑕佹坊鍔?Markdown 浠ｇ爜鍧楋級锛?{
  "dispatcher_summary": "<100瀛椾互鍐呯患鍚堝垎鏋?",
  "interventions": [
    {"role": "<瑙掕壊>", "reason": "<鍘熷洜锛屼腑鏂?", "priority": <1-3>}
  ]
}

鑻ュ綋鍓嶇姸鍐佃壇濂斤紝鏃犻渶骞查锛屽垯 interventions 杈撳嚭绌烘暟缁?[]銆?姣忔鏈€澶氬缓璁?2 涓共棰勪换鍔★紝閬垮厤 AI 鍒峰睆銆?```

---

### 姝ラ 3锛氬疄鐜颁笁浣嶅垎鏋愪笓瀹讹紙AI 鐗堟湰锛?
**鏂囦欢锛?* `backend/app/background_experts/cognitive_analyst.py`

```python
import json
from app.agents.llm_client import get_claude_client
from app.config import agent_settings
from app.analysis.metrics import extract_message_stats

class CognitiveAnalyst:
    MODEL = None  # 浠?agent_settings 璇诲彇
    
    def __init__(self):
        with open("app/agents/prompts/analysts/cognitive_analyst_prompt.txt") as f:
            self._prompt_template = f.read()
        self.MODEL = agent_settings.models.cognitive_analyst.model_version
        self.TOKEN_BUDGET = agent_settings.models.cognitive_analyst.history_token_budget
    
    async def analyze(
        self,
        messages: list[dict],
        task: dict,
        current_phase: str
    ) -> dict:
        """
        骞跺彂璋冪敤鍏ュ彛銆傝繑鍥炵粨鏋勫寲鍒嗘瀽缁撴灉銆?        """
        stats = extract_message_stats(messages)
        chat_history = self._format_history(messages)
        
        system_prompt = self._prompt_template.format(
            task_requirements=task.get("requirements", ""),
            current_phase=current_phase,
            participant_stats=json.dumps(stats, ensure_ascii=False),
            message_count=len(messages),
            chat_history=chat_history
        )
        
        client = get_claude_client()
        
        try:
            response = await client.messages.create(
                model=self.MODEL,
                max_tokens=1024,
                system=system_prompt,
                messages=[{"role": "user", "content": "璇疯緭鍑哄垎鏋愮粨鏋?JSON銆?}]
            )
            
            raw = response.content[0].text.strip()
            # 鍘婚櫎鍙兘鐨?Markdown 浠ｇ爜鍧楀寘瑁?            if raw.startswith("```"):
                raw = raw.split("```")[1]
                if raw.startswith("json"):
                    raw = raw[4:]
            
            result = json.loads(raw)
            return result
        
        except Exception as e:
            print(f"[CognitiveAnalyst] 鍒嗘瀽澶辫触: {e}")
            # 闄嶇骇锛氳繑鍥為粯璁ゅ€?            return {
                "diversity_score": 0.5,
                "progress_score": 0.5,
                "cognitive_report": {"diagnosis": "鍒嗘瀽鏆傛椂涓嶅彲鐢?, "error": str(e)}
            }
    
    def _format_history(self, messages: list[dict]) -> str:
        """鏍煎紡鍖栬亰澶╄褰曪紙鎸?Token Budget 鎴柇锛?""
        lines = []
        total_chars = 0
        # 姣忎釜 token 鈮?1.5 涓枃瀛楃锛岄浼?        char_budget = self.TOKEN_BUDGET * 1
        
        for msg in reversed(messages):  # 浠庢渶鏂版秷鎭紑濮?            line = f"[{msg.get('display_name', '?')}] {msg.get('content', '')}"
            if total_chars + len(line) > char_budget:
                lines.insert(0, "锛堜互涓嬩负鎸?token budget 鎴柇鍚庣殑杩戞湡娑堟伅锛?)
                break
            lines.insert(0, line)
            total_chars += len(line)
        
        return "\n".join(lines)
```

**`emotional_analyst.py` 鍜?`interaction_analyst.py` 缁撴瀯鐩稿悓锛屼粎 Prompt 鏂囦欢鍜岃緭鍑哄瓧娈典笉鍚岋紝姝ゅ鐪佺暐閲嶅浠ｇ爜銆?*

---

### 姝ラ 4锛氬疄鐜?ChiefDispatcher

**鏂囦欢锛?* `backend/app/background_experts/chief_dispatcher.py`

```python
import json
from app.agents.llm_client import get_claude_client
from app.config import agent_settings

class ChiefDispatcher:
    
    def __init__(self):
        with open("app/agents/prompts/analysts/chief_dispatcher_prompt.txt") as f:
            self._prompt_template = f.read()
        self.MODEL = agent_settings.models.chief_dispatcher.model_version
    
    async def dispatch(
        self,
        cognitive_report: dict,
        emotional_report: dict,
        interaction_report: dict,
        current_phase: str,
        elapsed_minutes: float
    ) -> dict:
        """
        鎺ユ敹涓変唤涓撳鎶ュ憡锛岃緭鍑鸿皟搴﹀喅绛栥€?        娉ㄦ剰锛欳hiefDispatcher 涓嶆帴鏀跺師濮嬭亰澶╄褰曘€?        """
        system_prompt = self._prompt_template.format(
            current_phase=current_phase,
            elapsed_minutes=f"{elapsed_minutes:.1f}",
            cognitive_report=json.dumps(cognitive_report, ensure_ascii=False),
            emotional_report=json.dumps(emotional_report, ensure_ascii=False),
            interaction_report=json.dumps(interaction_report, ensure_ascii=False)
        )
        
        client = get_claude_client()
        
        try:
            response = await client.messages.create(
                model=self.MODEL,
                max_tokens=512,
                system=system_prompt,
                messages=[{"role": "user", "content": "璇疯緭鍑鸿皟搴﹀喅绛?JSON銆?}]
            )
            
            raw = response.content[0].text.strip()
            if raw.startswith("```"):
                raw = raw.split("```")[1]
                if raw.startswith("json"):
                    raw = raw[4:]
            
            return json.loads(raw)
        
        except Exception as e:
            print(f"[ChiefDispatcher] 璋冨害澶辫触: {e}")
            return {
                "dispatcher_summary": "璋冨害鍒嗘瀽鏆傛椂涓嶅彲鐢?,
                "interventions": []
            }
```

---

### 姝ラ 5锛氬疄鐜?committee.py 缂栨帓灞傦紙鏍稿績锛?
**鏂囦欢锛?* `backend/app/agents/committee.py`锛圓I 鐗堟湰锛屾浛鎹?P4 鍩虹鐗堬級

```python
import asyncio, time, uuid
from datetime import datetime
from app.background_experts.cognitive_analyst import CognitiveAnalyst
from app.background_experts.emotional_analyst import EmotionalAnalyst
from app.background_experts.interaction_analyst import InteractionAnalyst
from app.background_experts.chief_dispatcher import ChiefDispatcher
from app.agents.queue import enqueue_task
from app.agents.context_builder import get_recent_messages, get_room_context
from app.db.session import AsyncSessionLocal
from app.models.analysis import AnalysisSnapshot, AgentIntervention, TriggeredBy
from app.analysis.metrics import calculate_participation_scores, calculate_balance_score

cognitive_analyst = CognitiveAnalyst()
emotional_analyst = EmotionalAnalyst()
interaction_analyst = InteractionAnalyst()
chief_dispatcher = ChiefDispatcher()


class ExpertCommittee:
    
    async def analyze_and_dispatch(
        self,
        room_id: str,
        triggered_by: TriggeredBy = TriggeredBy.timer
    ):
        """
        涓撳濮斿憳浼氬畬鏁村垎鏋愭祦绋嬨€?        
        鎵ц椤哄簭锛?        1. 鑾峰彇鑱婂ぉ璁板綍鍜屼笂涓嬫枃
        2. asyncio.gather 骞跺彂璋冪敤涓変綅 Analyst
        3. 灏嗕笁浣?Analyst 缁撴灉鍐欏叆 analysis_snapshots锛堝厛琛屽叆搴擄級
        4. 璋冪敤 ChiefDispatcher 缁煎悎瑁佸畾
        5. 鏇存柊 dispatcher_summary锛屽啓鍏?agent_interventions
        6. 灏嗚皟搴︿换鍔″啓鍏?Redis 闃熷垪
        """
        
        messages = await get_recent_messages(room_id, limit=50)
        context = await get_room_context(room_id)
        
        if not messages:
            return None
        
        # 鑾峰彇娑堟伅 seq_num 鑼冨洿
        seq_nums = [m.get("seq_num", 0) for m in messages if m.get("seq_num")]
        msg_seq_from = min(seq_nums) if seq_nums else 0
        msg_seq_to = max(seq_nums) if seq_nums else 0
        
        # 鑾峰彇浠诲姟淇℃伅
        task_info = {
            "requirements": context.get("task_description", ""),
            "scripts": context.get("task_scripts", "")
        }
        current_phase = context.get("current_phase", "")
        
        # ============ Step 2: 骞跺彂璋冪敤涓変綅 Analyst ============
        print(f"[Committee] 寮€濮嬪苟鍙戝垎鏋愶紝鎴块棿 {room_id}锛屾秷鎭暟 {len(messages)}")
        
        cognitive_result, emotional_result, interaction_result = await asyncio.gather(
            cognitive_analyst.analyze(messages, task_info, current_phase),
            emotional_analyst.analyze(messages, context.get("members_info", ""), current_phase),
            interaction_analyst.analyze(messages, context.get("members_info", ""), current_phase)
        )
        
        print(f"[Committee] 涓変綅 Analyst 骞跺彂瀹屾垚")
        
        # 璁＄畻缁熻鎸囨爣锛堢敤浜?participation_scores 鍜?balance_score锛?        participation = calculate_participation_scores(messages)
        balance = calculate_balance_score(participation)
        
        # ============ Step 3: 鍒涘缓蹇収璁板綍锛堜笁浣?Analyst 缁撴灉鍏堝叆搴擄級============
        snapshot_id = uuid.uuid4()
        
        async with AsyncSessionLocal() as db:
            snapshot = AnalysisSnapshot(
                id=snapshot_id,
                room_id=room_id,
                message_seq_from=msg_seq_from,
                message_seq_to=msg_seq_to,
                analyzed_message_count=len(messages),
                
                # CognitiveAnalyst 杈撳嚭
                diversity_score=cognitive_result.get("diversity_score", 0.5),
                progress_score=cognitive_result.get("progress_score", 0.5),
                cognitive_report=cognitive_result.get("cognitive_report", {}),
                
                # EmotionalAnalyst 杈撳嚭
                emotion_flags=emotional_result.get("emotion_flags", {}),
                emotional_report=emotional_result.get("emotional_report", {}),
                
                # InteractionAnalyst 杈撳嚭
                participation_scores=participation,
                balance_score=balance,
                interaction_report=interaction_result.get("interaction_report", {}),
                
                triggered_by=triggered_by,
            )
            db.add(snapshot)
            await db.commit()
        
        print(f"[Committee] 蹇収宸插啓鍏?DB锛宻napshot_id: {snapshot_id}")
        
        # ============ Step 4: ChiefDispatcher 缁煎悎瑁佸畾 ============
        room_created_at = context.get("room_created_at")
        elapsed = (datetime.utcnow() - room_created_at).total_seconds() / 60 if room_created_at else 0
        
        dispatcher_result = await chief_dispatcher.dispatch(
            cognitive_report=cognitive_result.get("cognitive_report", {}),
            emotional_report=emotional_result.get("emotional_report", {}),
            interaction_report=interaction_result.get("interaction_report", {}),
            current_phase=current_phase,
            elapsed_minutes=elapsed
        )
        
        # ============ Step 5: 鏇存柊 dispatcher_summary锛屽啓鍏ュ共棰勮褰?============
        interventions_to_queue = dispatcher_result.get("interventions", [])
        
        async with AsyncSessionLocal() as db:
            # 鏇存柊蹇収鐨?dispatcher_summary
            from sqlalchemy import update
            await db.execute(
                update(AnalysisSnapshot)
                .where(AnalysisSnapshot.id == snapshot_id)
                .values(dispatcher_summary=dispatcher_result.get("dispatcher_summary", ""))
            )
            
            # 鍐欏叆 agent_interventions 璁板綍
            for intervention in interventions_to_queue:
                ai = AgentIntervention(
                    room_id=room_id,
                    snapshot_id=snapshot_id,
                    agent_role=intervention["role"],
                    reason=intervention["reason"],
                )
                db.add(ai)
            
            await db.commit()
        
        # ============ Step 6: 灏嗚皟搴︿换鍔″啓鍏?Redis 闃熷垪 ============
        for intervention in interventions_to_queue:
            await enqueue_task(room_id, {
                "room_id": room_id,
                "agent_role": intervention["role"],
                "reason": intervention["reason"],
                "priority": intervention.get("priority", 2),
                "snapshot_id": str(snapshot_id),
                "triggered_at": time.time()
            })
        
        print(f"[Committee] 鍒嗘瀽瀹屾垚锛岀敓鎴?{len(interventions_to_queue)} 涓共棰勪换鍔?)
        return str(snapshot_id)
    
    async def check_b_class_triggers(
        self,
        snapshot_id: str,
        cognitive_result: dict,
        emotional_result: dict,
        interaction_result: dict,
        room_id: str
    ):
        """B 绫诲垎鏋愮粨鏋滈┍鍔ㄨ皟搴︼紙鍦?ChiefDispatcher 涔嬪鐨勯澶栬鍒欐娴嬶級"""
        from app.config import agent_settings
        
        thresholds = agent_settings.thresholds
        
        # B 绫伙細瑙傜偣瓒嬪悓
        if cognitive_result.get("diversity_score", 1.0) < thresholds.diversity_score_threshold:
            pass  # 宸茬敱 ChiefDispatcher 澶勭悊锛屾澶勫彲娣诲姞棰濆閫昏緫
        
        # B 绫伙細鎯呯华鍐茬獊
        flags = emotional_result.get("emotion_flags", {})
        if flags.get("conflict"):
            pass  # 鍚屼笂


expert_committee = ExpertCommittee()
```

---

### 姝ラ 6锛氬畬鏁?C 绫讳富鍔ㄥ彫鍞よЕ鍙?
鍦?`handlers.py` 涓畬鍠?@Agent 瑙﹀彂閫昏緫锛堝惈 `agent_interventions` 鍐欏叆锛夛細

```python
async def handle_chat_message(data, room_id, user, db):
    content = data.get("content", "").strip()
    mentions = data.get("mentions", [])
    
    msg = await MessageService.save_student_message(db, room_id, str(user.id), content)
    await manager.broadcast_to_room(room_id, {...})  # 骞挎挱娑堟伅
    
    # A 绫绘娴?    await trigger_detector.check_monopoly(room_id, str(user.id))
    
    # C 绫昏Е鍙戯細@Agent 涓诲姩鍙敜
    if mentions and agent_settings.mention.enabled:
        valid_roles = list(ROLE_AGENTS.keys())
        for role in mentions[:agent_settings.mention.max_mentions_per_message]:
            if role not in valid_roles:
                continue
            
            # 鍐欏叆 agent_interventions锛圕 绫伙紝snapshot_id=null锛?            intervention = AgentIntervention(
                room_id=room_id,
                snapshot_id=None,  # C 绫讳笉鍏宠仈蹇収
                agent_role=role,
                reason=f"瀛︾敓 {user.display_name} 閫氳繃 @ 涓诲姩鍙敜锛歿content[:100]}"
            )
            db.add(intervention)
            await db.commit()
            
            # 鍐欏叆 priority=0 鐨勯槦鍒椾换鍔?            await enqueue_task(room_id, {
                "room_id": room_id,
                "agent_role": role,
                "reason": content,
                "priority": agent_settings.mention.priority,
                "trigger_type": "mention",
                "student_name": user.display_name,
                "triggered_at": time.time()
            })
```

---

### 姝ラ 7锛氬疄鐜板垎鏋愭暟鎹?REST API

**鏂囦欢锛?* `backend/app/routers/analytics.py`

```python
from fastapi import APIRouter, Depends
from app.models.analysis import AnalysisSnapshot, AgentIntervention

analytics_router = APIRouter(prefix="/api/analytics", tags=["鍒嗘瀽"])

@analytics_router.get("/rooms/{room_id}/snapshots")
async def get_snapshots(
    room_id: UUID,
    limit: int = Query(20, le=50),
    current_user: User = Depends(require_teacher),
    db: AsyncSession = Depends(get_db)
):
    """鑾峰彇鍗忎綔鍒嗘瀽蹇収鍒楄〃锛堟暀甯堢涓撶敤锛?""
    result = await db.execute(
        select(AnalysisSnapshot)
        .where(AnalysisSnapshot.room_id == room_id)
        .order_by(AnalysisSnapshot.created_at.desc())
        .limit(limit)
    )
    snapshots = result.scalars().all()
    return {"snapshots": snapshots}


@analytics_router.get("/rooms/{room_id}/interventions")
async def get_interventions(
    room_id: UUID,
    limit: int = Query(50, le=100),
    current_user: User = Depends(require_teacher),
    db: AsyncSession = Depends(get_db)
):
    """鑾峰彇骞查璁板綍鍒楄〃锛堟暀甯堢涓撶敤锛?""
    result = await db.execute(
        select(AgentIntervention)
        .where(AgentIntervention.room_id == room_id)
        .order_by(AgentIntervention.created_at.desc())
        .limit(limit)
    )
    interventions = result.scalars().all()
    return {"interventions": interventions}


@analytics_router.get("/rooms/{room_id}/summary")
async def get_summary(
    room_id: UUID,
    current_user: User = Depends(require_teacher),
    db: AsyncSession = Depends(get_db)
):
    """鑾峰彇鏁翠綋鍗忎綔鎽樿锛堟渶鏂板揩鐓х殑缁煎悎鎸囨爣锛?""
    latest = await db.execute(
        select(AnalysisSnapshot)
        .where(AnalysisSnapshot.room_id == room_id)
        .order_by(AnalysisSnapshot.created_at.desc())
        .limit(1)
    )
    snapshot = latest.scalar_one_or_none()
    
    if not snapshot:
        return {"message": "鏆傛棤鍒嗘瀽鏁版嵁"}
    
    return {
        "diversity_score": snapshot.diversity_score,
        "balance_score": snapshot.balance_score,
        "progress_score": snapshot.progress_score,
        "emotion_flags": snapshot.emotion_flags,
        "dispatcher_summary": snapshot.dispatcher_summary,
        "last_analysis_at": snapshot.created_at.isoformat()
    }
```

---

### 姝ラ 8锛氬疄鐜?analysis:update WebSocket 鎺ㄩ€?
鍦?`committee.py` 鐨?`analyze_and_dispatch` 瀹屾垚鍚庯紝鍚戞暀甯堢鎺ㄩ€佹洿鏂帮細

```python
# committee.py 鏈熬杩藉姞
from app.websocket.manager import manager

async def _push_to_teachers(self, room_id: str, snapshot_id: str):
    """鍚戞暀甯堢鎺ㄩ€佹渶鏂板垎鏋愬揩鐓?""
    async with AsyncSessionLocal() as db:
        snapshot = await db.get(AnalysisSnapshot, snapshot_id)
    
    await manager.broadcast_to_teachers(room_id, {
        "type": "analysis:update",
        "snapshot": {
            "id": str(snapshot.id),
            "diversity_score": snapshot.diversity_score,
            "balance_score": snapshot.balance_score,
            "progress_score": snapshot.progress_score,
            "emotion_flags": snapshot.emotion_flags,
            "cognitive_report": snapshot.cognitive_report,
            "emotional_report": snapshot.emotional_report,
            "interaction_report": snapshot.interaction_report,
            "dispatcher_summary": snapshot.dispatcher_summary,
            "created_at": snapshot.created_at.isoformat()
        }
    })
```

---

### 姝ラ 9锛氶槻閲嶅瑙﹀彂鏈哄埗瀹屽杽

```python
# triggers.py 闃查噸澶嶆満鍒舵€昏

# A 绫伙細鍚屾瑙勫垯锛堝瀯鏂級
trigger_lock::{room_id}::monopoly    TTL=60s

# A 绫伙細杞瑙勫垯锛堟矇榛橈級
trigger_lock::{room_id}::silence     TTL=60s

# 瀹氭椂鍒嗘瀽锛堜笓瀹跺鍛樹細锛?analysis_running::{room_id}          TTL=120s锛堝垎鏋愭墽琛屾湡闂村姞閿侊級

# B 绫伙紙蹇収澶勭悊瀹屽悗涓嶉噸澶嶈拷鍔狅級
# 閫氳繃 snapshot 鐨勫敮涓€鎬т繚璇侊紝鍚屼竴 snapshot_id 鍙啓涓€娆?```

---

## 浜斻€乄ebSocket 浜嬩欢鍗忚锛圥5 鏂板锛?
| 浜嬩欢鍚?| 鏂瑰悜 | 璇存槑 |
|--------|------|------|
| `analysis:update` | 鏈嶁啋鏁欏笀绔?| 鎼哄甫瀹屾暣蹇収鏁版嵁锛屽惈涓変唤涓撳鎶ュ憡 |

> `analysis:update` 鍙彂閫佺粰 `role=teacher` 鐨?WebSocket 杩炴帴锛屽鐢熺涓嶆帴鏀躲€?
---

## 鍏€佹紨绀轰氦浠樼墿

**P5 婕旂ず鐗堟湰 鈥?鏅鸿兘鍒嗘瀽骞查 Demo**

**婕旂ず姝ラ锛?*
1. 瀛︾敓浠繘琛屼竴娈佃璁猴紙鍙互鏄璁捐剼鏈紝妯℃嫙"瑙傜偣瓒嬪悓"鍦烘櫙锛?2. 鎵嬪姩璋冪敤 `/api/debug/trigger-committee?room_id=...` 瑙﹀彂涓€娆″垎鏋?3. 鍚庡彴鏃ュ織鏄剧ず锛歚[Committee] 寮€濮嬪苟鍙戝垎鏋?鈫?涓変綅 Analyst 骞跺彂瀹屾垚 鈫?蹇収宸插啓鍏?DB`
4. 鏁欏笀绔疄鏃舵敹鍒?`analysis:update` 鎺ㄩ€侊紝鏄剧ず鍖呭惈涓変唤涓撳鎶ュ憡鐨勫垎鏋愬揩鐓?5. 婕旂ず C 绫昏Е鍙戯細瀛︾敓杈撳叆 `@璧勬簮鑰?璇峰府鎴戞壘鐩稿叧妗堜緥`锛岃祫婧愯€呯珛鍗冲搷搴旓紙涓嶇瓑寰呭垎鏋愶級

**楠屾敹鎸囨爣锛?*
- [ ] 涓変綅 Analyst 閫氳繃 `asyncio.gather` 骞跺彂璋冪敤锛堟棩蹇楁樉绀?骞跺彂瀹屾垚"锛?- [ ] 鍒嗘瀽蹇収鍐欏叆鏁版嵁搴擄紝鍖呭惈 `cognitive_report`銆乣emotional_report`銆乣interaction_report` 瀛楁
- [ ] `dispatcher_summary` 瀛楁鏈夊唴瀹癸紙ChiefDispatcher 杈撳嚭锛?- [ ] C 绫?@Agent 鍦?10 绉掑唴鍝嶅簲锛屼笉绛夊緟涓撳鍒嗘瀽
- [ ] 鏁欏笀绔敹鍒?`analysis:update` 浜嬩欢锛堝彲鐢?WebSocket 璋冭瘯宸ュ叿楠岃瘉锛?- [ ] 闃查噸澶嶈Е鍙戯細鍚屼竴鏉′欢1鍒嗛挓鍐呬笉閲嶅婵€娲伙紙鏃ュ織涓嶉噸澶嶆墦鍗帮級
- [ ] Claude API 澶辫触鏃舵湁闄嶇骇杈撳嚭锛堜笉宕╂簝锛?
---

## 涓冦€丳5 鈫?P6 浜ゆ帴璇存槑

P5 缁撴潫鍚庯細

- 鉁?涓撳濮斿憳浼?AI 鍒嗘瀽閾捐矾瀹屽叏鎵撻€?- 鉁?`analysis_snapshots` 鍜?`agent_interventions` 鏁版嵁姝ｅ父绉疮
- 鉁?鏁欏笀绔?WebSocket 鍒嗘瀽鎺ㄩ€佸凡灏辩华
- 鉁?A/B/C 涓夌被瑙﹀彂鏈哄埗鍧囧凡瀹炵幇骞舵祴璇?
P6 闃舵涓撴敞鍓嶇 UI锛歍iptap + Yjs 鍗忓悓缂栬緫銆佹秷鎭牱寮忕簿淇€丂Mention 杈撳叆妗嗭紝涓嶆柊澧炲悗绔姛鑳姐€?